language: node_js
node_js:
  - "6.10"

before_script:
  - npm install -g @angular/cli
  - npm install -g karma
  - npm install -g gulp
  - npm install

notifications:
  email:
    recipients:
      - dcampos@vmware.com
      - juli@vmware.com
      - aspear@vmware.com
    on_success: never # default: change
    on_failure: always # default: always
